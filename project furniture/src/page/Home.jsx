import Header from "../components/Header";
import HeroSection from "../components/HeroSection";
// Import other components once created

const Home = () => {
  return (
    <>
      <Header />
      <HeroSection />
      {/* Add other components here like <Collection />, <Categories />, etc. */}
    </>
  );
};

export default Home;
